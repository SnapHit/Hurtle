# reference

Source material, not deployed. Only public/ is served.

index.html is the live snap-hit.online homepage, kept so the
arcade cabinet in the cluster pages can be diffed against
its original if either changes.
